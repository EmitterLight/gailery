[pytest]
markers =
    gpu: tests that require GPU (llama-server / InsightFace)
    ai: tests that call AI models (embed, search, describe, enrich)
    write: tests that write to database (require minidb fixture)
